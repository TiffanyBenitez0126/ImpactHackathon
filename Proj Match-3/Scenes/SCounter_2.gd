extends TextureRect

onready var scounter2_label = $SCounter_2Label


#var mascurrent_count = preload("res://Scripts/SCounter.gd")
#print("mascurrent_count")
# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	#var current_scount2 = current_scount.new()
	#scounter2_label.text = "Total pts.: " + str(current_scount2)
	pass
# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
